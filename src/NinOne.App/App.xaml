<Application x:Class="NinOne.App.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Startup="OnStartup">
    <Application.Resources>
        <SolidColorBrush x:Key="AppBackground" Color="#FFF1F4F7" />
        <SolidColorBrush x:Key="SurfaceBackground" Color="#FFFFFFFF" />
        <SolidColorBrush x:Key="SurfaceBorder" Color="#FFD7DEE6" />
        <SolidColorBrush x:Key="TextPrimary" Color="#FF172A3A" />
        <SolidColorBrush x:Key="TextSecondary" Color="#FF607386" />
        <SolidColorBrush x:Key="Accent" Color="#FF1473A8" />
        <SolidColorBrush x:Key="AccentDark" Color="#FF14334A" />
        <SolidColorBrush x:Key="Success" Color="#FF18864B" />
        <SolidColorBrush x:Key="Warning" Color="#FFD88916" />
        <SolidColorBrush x:Key="Danger" Color="#FFC23B36" />
        <SolidColorBrush x:Key="InputBackground" Color="#FFFBFCFD" />

        <Style TargetType="{x:Type Window}">
            <Setter Property="FontFamily" Value="Microsoft YaHei UI" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}" />
            <Setter Property="Background" Value="{StaticResource AppBackground}" />
        </Style>
        <Style TargetType="{x:Type Button}">
            <Setter Property="MinWidth" Value="84" />
            <Setter Property="Height" Value="34" />
            <Setter Property="Margin" Value="4" />
            <Setter Property="Padding" Value="12,4" />
            <Setter Property="Background" Value="#FFE8EEF4" />
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}" />
            <Setter Property="BorderBrush" Value="#FFCAD4DE" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True"><Setter Property="Background" Value="#FFDCE8F1" /></Trigger>
                <Trigger Property="IsEnabled" Value="False"><Setter Property="Opacity" Value="0.55" /></Trigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="PrimaryButton" TargetType="{x:Type Button}" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background" Value="{StaticResource Accent}" /><Setter Property="BorderBrush" Value="{StaticResource Accent}" /><Setter Property="Foreground" Value="White" />
        </Style>
        <Style x:Key="SuccessButton" TargetType="{x:Type Button}" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background" Value="{StaticResource Success}" /><Setter Property="BorderBrush" Value="{StaticResource Success}" /><Setter Property="Foreground" Value="White" />
        </Style>
        <Style x:Key="DangerButton" TargetType="{x:Type Button}" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background" Value="{StaticResource Danger}" /><Setter Property="BorderBrush" Value="{StaticResource Danger}" /><Setter Property="Foreground" Value="White" />
        </Style>
        <Style x:Key="WarningButton" TargetType="{x:Type Button}" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background" Value="#FFFFF1D6" /><Setter Property="BorderBrush" Value="#FFE2B356" /><Setter Property="Foreground" Value="#FF7A4A00" />
        </Style>
        <Style TargetType="{x:Type TextBox}">
            <Setter Property="Height" Value="32" /><Setter Property="Margin" Value="4" /><Setter Property="Padding" Value="7,4" />
            <Setter Property="Background" Value="{StaticResource InputBackground}" /><Setter Property="BorderBrush" Value="#FFBAC6D1" /><Setter Property="BorderThickness" Value="1" /><Setter Property="VerticalContentAlignment" Value="Center" />
        </Style>
        <Style TargetType="{x:Type ComboBox}">
            <Setter Property="Height" Value="32" /><Setter Property="Margin" Value="4" /><Setter Property="Padding" Value="6,2" />
            <Setter Property="Background" Value="{StaticResource InputBackground}" /><Setter Property="BorderBrush" Value="#FFBAC6D1" /><Setter Property="VerticalContentAlignment" Value="Center" />
        </Style>
        <Style TargetType="{x:Type CheckBox}"><Setter Property="Margin" Value="6,4" /><Setter Property="VerticalAlignment" Value="Center" /></Style>
        <Style TargetType="{x:Type GroupBox}">
            <Setter Property="Margin" Value="0,0,0,12" /><Setter Property="Padding" Value="12" /><Setter Property="Background" Value="{StaticResource SurfaceBackground}" />
            <Setter Property="BorderBrush" Value="{StaticResource SurfaceBorder}" /><Setter Property="BorderThickness" Value="1" />
            <Setter Property="HeaderTemplate"><Setter.Value><DataTemplate><TextBlock Text="{Binding}" FontWeight="SemiBold" Foreground="{StaticResource TextPrimary}" /></DataTemplate></Setter.Value></Setter>
        </Style>
        <Style TargetType="{x:Type Expander}"><Setter Property="Margin" Value="0,8,0,0" /><Setter Property="FontWeight" Value="SemiBold" /></Style>

        <Style x:Key="CardStyle" TargetType="{x:Type Border}">
            <Setter Property="Background" Value="{StaticResource SurfaceBackground}" /><Setter Property="BorderBrush" Value="{StaticResource SurfaceBorder}" /><Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius" Value="5" /><Setter Property="Padding" Value="14" /><Setter Property="Margin" Value="0,0,0,12" />
        </Style>
        <Style x:Key="StatusStyle" TargetType="{x:Type Border}">
            <Setter Property="CornerRadius" Value="12" /><Setter Property="Padding" Value="10,5" /><Setter Property="Background" Value="#FFE8EEF4" />
            <Setter Property="BorderBrush" Value="#FFD2DCE5" /><Setter Property="BorderThickness" Value="1" /><Setter Property="Margin" Value="4" />
        </Style>
        <Style x:Key="LabelStyle" TargetType="{x:Type TextBlock}">
            <Setter Property="VerticalAlignment" Value="Center" /><Setter Property="Margin" Value="8,4,4,4" /><Setter Property="Foreground" Value="{StaticResource TextSecondary}" />
        </Style>
        <Style x:Key="ResultStyle" TargetType="{x:Type TextBlock}"><Setter Property="Margin" Value="8,4" /><Setter Property="TextWrapping" Value="Wrap" /></Style>
        <Style x:Key="PageTitleStyle" TargetType="{x:Type TextBlock}"><Setter Property="FontSize" Value="22" /><Setter Property="FontWeight" Value="SemiBold" /><Setter Property="Foreground" Value="{StaticResource TextPrimary}" /><Setter Property="Margin" Value="0,0,0,4" /></Style>
        <Style x:Key="PageSubtitleStyle" TargetType="{x:Type TextBlock}"><Setter Property="Foreground" Value="{StaticResource TextSecondary}" /><Setter Property="TextWrapping" Value="Wrap" /><Setter Property="Margin" Value="0,0,0,14" /></Style>
        <Style x:Key="SectionTitleStyle" TargetType="{x:Type TextBlock}"><Setter Property="FontSize" Value="15" /><Setter Property="FontWeight" Value="SemiBold" /><Setter Property="Margin" Value="0,0,0,10" /></Style>
        <Style x:Key="RawStyle" TargetType="{x:Type TextBox}" BasedOn="{StaticResource {x:Type TextBox}}">
            <Setter Property="IsReadOnly" Value="True" /><Setter Property="AcceptsReturn" Value="True" /><Setter Property="VerticalScrollBarVisibility" Value="Auto" /><Setter Property="HorizontalScrollBarVisibility" Value="Auto" />
            <Setter Property="FontFamily" Value="Consolas" /><Setter Property="FontSize" Value="12" /><Setter Property="Height" Value="150" /><Setter Property="VerticalContentAlignment" Value="Top" />
        </Style>

        <Style x:Key="NavTabItemStyle" TargetType="{x:Type TabItem}">
            <Setter Property="FontSize" Value="13" /><Setter Property="Height" Value="46" /><Setter Property="Margin" Value="10,2" /><Setter Property="HorizontalContentAlignment" Value="Stretch" /><Setter Property="VerticalContentAlignment" Value="Stretch" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type TabItem}">
                        <Border x:Name="NavItem" Background="Transparent" CornerRadius="4" Padding="14,0"><ContentPresenter x:Name="HeaderContent" ContentSource="Header" HorizontalAlignment="Left" VerticalAlignment="Center" TextElement.Foreground="#FFC7D3DD" TextElement.FontWeight="Normal" /></Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True"><Setter TargetName="NavItem" Property="Background" Value="{StaticResource Accent}" /><Setter TargetName="HeaderContent" Property="TextElement.Foreground" Value="White" /><Setter TargetName="HeaderContent" Property="TextElement.FontWeight" Value="SemiBold" /></Trigger>
                            <MultiTrigger><MultiTrigger.Conditions><Condition Property="IsSelected" Value="False" /><Condition Property="IsMouseOver" Value="True" /></MultiTrigger.Conditions><Setter TargetName="NavItem" Property="Background" Value="#FF234B67" /></MultiTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="NavTabControlStyle" TargetType="{x:Type TabControl}">
            <Setter Property="ItemContainerStyle" Value="{StaticResource NavTabItemStyle}" /><Setter Property="Background" Value="{StaticResource AppBackground}" /><Setter Property="BorderThickness" Value="0" />
            <Setter Property="HorizontalContentAlignment" Value="Stretch" /><Setter Property="VerticalContentAlignment" Value="Stretch" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type TabControl}">
                        <Grid KeyboardNavigation.TabNavigation="Local">
                            <Grid.ColumnDefinitions><ColumnDefinition Width="220" /><ColumnDefinition Width="*" /></Grid.ColumnDefinitions>
                            <Border Background="{StaticResource AccentDark}">
                                <DockPanel LastChildFill="True">
                                    <StackPanel DockPanel.Dock="Top" Margin="20,18,16,12"><TextBlock Text="功能导航" Foreground="White" FontSize="15" FontWeight="SemiBold" /><TextBlock Text="设备与工程工具" Foreground="#FF93AABE" FontSize="12" Margin="0,3,0,0" /></StackPanel>
                                    <Separator DockPanel.Dock="Top" Margin="16,0,16,8" Background="#FF315269" />
                                    <TabPanel IsItemsHost="True" KeyboardNavigation.TabIndex="1" />
                                </DockPanel>
                            </Border>
                            <Border Grid.Column="1" Background="{StaticResource AppBackground}"><ContentPresenter x:Name="PART_SelectedContentHost" ContentSource="SelectedContent" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" TextElement.Foreground="{StaticResource TextPrimary}" TextElement.FontWeight="Normal" /></Border>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Application.Resources>
</Application>
